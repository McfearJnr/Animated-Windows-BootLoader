<Page
    x:Class="HackBGRTAnimated.WinUI.Views.ImportGifPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource AppBackgroundBrush}"
    AllowDrop="True"
    DragOver="Page_DragOver"
    DragLeave="Page_DragLeave"
    Drop="Page_Drop">

    <ScrollViewer>
        <StackPanel Padding="20" Spacing="14">
            <Border Style="{StaticResource AppCardBorderStyle}" Background="{StaticResource AppSurfaceAltBrush}">
                <StackPanel Spacing="6">
                    <TextBlock Text="Import GIF" Style="{StaticResource PageTitleTextStyle}"/>
                    <TextBlock Text="Import an animated GIF and convert it into BMP frames for UEFI playback." Style="{StaticResource MutedTextStyle}"/>
                </StackPanel>
            </Border>

            <Border x:Name="DropZoneBorder" Style="{StaticResource AppCardBorderStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Drag and drop GIF" Style="{StaticResource SectionTitleTextStyle}"/>
                    <TextBlock Text="Drop a .gif file here, or pick one manually." Style="{StaticResource MutedTextStyle}"/>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBox x:Name="GifPathBox" Width="700" PlaceholderText="Select or drop a .gif file"/>
                        <Button Content="Browse" Style="{StaticResource SecondaryActionButtonStyle}" Click="Browse_OnClick"/>
                    </StackPanel>
                    <Border Style="{StaticResource AppCardMutedBorderStyle}">
                        <Image x:Name="GifPreview" Height="220" Stretch="Uniform"/>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource AppCardBorderStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Import Options" Style="{StaticResource SectionTitleTextStyle}"/>
                    <Grid ColumnSpacing="10" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="280"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Theme name" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="ThemeNameBox"/>

                        <TextBlock Grid.Row="1" Text="Output width" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="WidthBox" Text="400"/>

                        <TextBlock Grid.Row="2" Text="Output height" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" x:Name="HeightBox" Text="400"/>

                        <TextBlock Grid.Row="3" Text="FPS" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="3" Grid.Column="1" x:Name="FpsBox" Text="15"/>

                        <TextBlock Grid.Row="4" Text="Max duration (ms)" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="4" Grid.Column="1" x:Name="DurationBox" Text="3000"/>

                        <TextBlock Grid.Row="5" Text="Background (RRGGBB)" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="5" Grid.Column="1" x:Name="BackgroundBox" Text="000000"/>
                    </Grid>

                    <CheckBox x:Name="SetActiveCheck" Content="Set imported theme as active" IsChecked="True"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource AppCardBorderStyle}">
                <StackPanel Spacing="10">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button x:Name="ImportButton" Content="Import GIF" Style="{StaticResource PrimaryActionButtonStyle}" Click="Import_OnClick"/>
                        <Button Content="Open Themes Folder" Style="{StaticResource SecondaryActionButtonStyle}" Click="OpenThemesFolder_OnClick"/>
                    </StackPanel>

                    <ProgressBar x:Name="ImportProgressBar" Minimum="0" Maximum="100" Height="8" Visibility="Collapsed" IsIndeterminate="True"/>
                    <TextBlock Text="Import Progress" Style="{StaticResource SectionTitleTextStyle}"/>
                    <TextBox x:Name="ProgressLog" Height="220" AcceptsReturn="True" IsReadOnly="True" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
