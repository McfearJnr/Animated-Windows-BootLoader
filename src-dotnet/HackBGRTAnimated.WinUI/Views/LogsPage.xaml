<Page
    x:Class="HackBGRTAnimated.WinUI.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource AppBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="20,20,20,12" Style="{StaticResource AppCardBorderStyle}" Background="{StaticResource AppSurfaceAltBrush}">
            <StackPanel Spacing="6">
                <TextBlock Text="Logs" Style="{StaticResource PageTitleTextStyle}"/>
                <TextBlock x:Name="LogsPathText" Style="{StaticResource MutedTextStyle}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="20,0,20,10" Style="{StaticResource AppCardBorderStyle}">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <ComboBox x:Name="LogTypeCombo" Width="220" SelectionChanged="LogTypeCombo_OnSelectionChanged">
                    <ComboBoxItem Content="Boot Log"/>
                    <ComboBoxItem Content="Setup Log"/>
                    <ComboBoxItem Content="Action Log"/>
                    <ComboBoxItem Content="GUI Log"/>
                </ComboBox>
                <Button Content="Refresh" Style="{StaticResource SecondaryActionButtonStyle}" Click="Refresh_OnClick"/>
                <Button Content="Copy Log" Style="{StaticResource GhostActionButtonStyle}" Click="CopyLog_OnClick"/>
                <Button Content="Open Logs Folder" Style="{StaticResource GhostActionButtonStyle}" Click="OpenLogsFolder_OnClick"/>
                <TextBlock x:Name="LogStatusText" Margin="8,0,0,0" Style="{StaticResource MutedTextStyle}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Margin="20,0,20,12" Style="{StaticResource AppCardBorderStyle}">
            <TextBox x:Name="LogContentBox" AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True" ScrollViewer.VerticalScrollBarVisibility="Auto" Height="420" />
        </Border>

        <Border Grid.Row="3" Margin="20,0,20,20" Style="{StaticResource AppCardMutedBorderStyle}">
            <TextBlock Text="Failed commands and errors are shown directly in the selected log content." Style="{StaticResource MutedTextStyle}"/>
        </Border>
    </Grid>
</Page>
