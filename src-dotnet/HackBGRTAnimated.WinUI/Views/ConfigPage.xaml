<Page
    x:Class="HackBGRTAnimated.WinUI.Views.ConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:HackBGRTAnimated.WinUI.Controls"
    mc:Ignorable="d"
    Background="{StaticResource AppBackgroundBrush}">

    <ScrollViewer>
        <StackPanel Padding="20" Spacing="12">
            <Border Style="{StaticResource AppCardBorderStyle}" Background="{StaticResource AppSurfaceAltBrush}">
                <StackPanel Spacing="6">
                    <TextBlock Text="Config" Style="{StaticResource PageTitleTextStyle}"/>
                    <TextBlock x:Name="ActiveThemeText" Style="{StaticResource HeroSubHeadingTextStyle}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource AppCardBorderStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Animation Settings" Style="{StaticResource SectionTitleTextStyle}"/>

                    <controls:SettingsRow Title="FPS" Description="Animation playback speed for the active theme.">
                        <controls:SettingsRow.SettingControl>
                            <TextBox x:Name="FpsBox" Width="160"/>
                        </controls:SettingsRow.SettingControl>
                    </controls:SettingsRow>

                    <controls:SettingsRow Title="Duration (ms)" Description="Maximum animation duration before fallback behavior.">
                        <controls:SettingsRow.SettingControl>
                            <TextBox x:Name="DurationBox" Width="160"/>
                        </controls:SettingsRow.SettingControl>
                    </controls:SettingsRow>

                    <controls:SettingsRow Title="Preload Frames" Description="Load animation frames into memory before playback.">
                        <controls:SettingsRow.SettingControl>
                            <ToggleSwitch x:Name="PreloadToggle"/>
                        </controls:SettingsRow.SettingControl>
                    </controls:SettingsRow>

                    <controls:SettingsRow Title="Clear Each Frame" Description="Clear frame buffer before drawing the next frame.">
                        <controls:SettingsRow.SettingControl>
                            <ToggleSwitch x:Name="ClearEachFrameToggle"/>
                        </controls:SettingsRow.SettingControl>
                    </controls:SettingsRow>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource AppCardBorderStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Safety / Fallback" Style="{StaticResource SectionTitleTextStyle}"/>
                    <controls:SettingsRow Title="Panic Key" Description="Key used to bypass animation on boot.">
                        <controls:SettingsRow.SettingControl>
                            <ComboBox x:Name="PanicKeyCombo" Width="180">
                                <ComboBoxItem Content="esc"/>
                                <ComboBoxItem Content="space"/>
                                <ComboBoxItem Content="none"/>
                            </ComboBox>
                        </controls:SettingsRow.SettingControl>
                    </controls:SettingsRow>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource AppCardBorderStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Actions" Style="{StaticResource SectionTitleTextStyle}"/>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Save Settings" Style="{StaticResource PrimaryActionButtonStyle}" Click="Save_OnClick"/>
                        <Button Content="Set Static Fallback Image" Style="{StaticResource SecondaryActionButtonStyle}" Click="SetFallbackImage_OnClick"/>
                        <Button Content="Refresh" Style="{StaticResource GhostActionButtonStyle}" Click="Refresh_OnClick"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Open AppData Folder" Style="{StaticResource GhostActionButtonStyle}" Click="OpenAppData_OnClick"/>
                        <Button Content="Open EFI Folder" Style="{StaticResource GhostActionButtonStyle}" Click="OpenEfi_OnClick"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
