<UserControl
    x:Class="HackBGRTAnimated.WinUI.Controls.SettingsRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="Root">

    <Border Style="{StaticResource AppCardMutedBorderStyle}" Padding="14">
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="4">
                <TextBlock Text="{Binding Title, ElementName=Root}" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Description, ElementName=Root}" Style="{StaticResource MutedTextStyle}"/>
            </StackPanel>

            <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Content="{Binding SettingControl, ElementName=Root}"/>
        </Grid>
    </Border>
</UserControl>
